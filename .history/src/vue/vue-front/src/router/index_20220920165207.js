/*import Vue from 'vue'
import Router from 'vue-router'


Vue.use(Router)

const routes= [
  {
    path: '/todo',
    name: 'todo',
    redirect:'/todo',
  }
];

  const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
*/
